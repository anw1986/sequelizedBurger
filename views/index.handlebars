<div class="container">
    <div class="jumbotron">

        <h1>Burgers To Devour</h1>

    </div>

    <div class="row">

        <div class="col-3">
            <h6>Burgers on the Menu</h6>
        </div>
        <div class="col-1">
            {{!-- <h6>Temperature</h6> --}}
        </div>
        <div class="col-2">
            <h6>Action</h6>
        </div>
        <div class="col-3">
            <h6>Burgers devoured</h6>
        </div>
        <div class="col-1">
            {{!-- <h6>Temperature</h6> --}}
        </div>
        <div class="col-2">
            <h6>Action</h6>
        </div>

    </div>

    {{!-- <div class="row">
        <div class="col-4">
            <h6>Burgers on the Menu</h6>
        </div>
        <div class="col-1"></div>
        <div class="col-2">
        </div>
        <div class="col-1"></div>
        <div class="col-4">
            <h6>Burgers to devour</h6>
        </div>
    </div> --}}

    {{!-- <div class="row">
        <div class="col-4">
            <ul>
                {{#each result}}
                    {{#unless dataValues.burger_devoured}}
                        <li>
                            {{dataValues.burger_name}}
                        </li> --}}
    {{!-- {{>burger/burger-block devoured=1}} --}}
    {{!-- {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-1"></div>
        <div class="col-2">
            <img src="/assets/img/burger.png" class="img-fluid">
        </div>
        <div class="col-1"></div>
        <div class="col-4">
            <ul>
                {{#each result}}
                    {{#if dataValues.burger_devoured}}
                        {{>burger/burger-block devoured=0}}
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div> --}}

    <div class="row">
        <div class="col-3">
            <ul>
                {{#each result}}
                {{#unless dataValues.burger_devoured}}
                <li>
                    {{dataValues.burger_name}}
                </li>

                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-1"></div>
        {{!-- Need Handle Bars over here for temperatur drop down --}}
        <div class="col-2">
            {{#each result}}
            {{#unless dataValues.burger_devoured}}
            {{>burger/burger-block devoured=1}}
            {{/unless}}
            {{/each}}
        </div>

        <div class="col-3">

            <ul>
                {{#each result}}
                {{#if dataValues.burger_devoured}}
                <li>
                    {{dataValues.burger_name}}
                </li>

                {{/if}}
                {{/each}}
            </ul>

        </div>
        <div class="col-1"></div>
        <div class="col-2">
            <ul>
                {{#each result}}
                {{#if dataValues.burger_devoured}}
                {{>burger/burger-block devoured=0}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>

    <h2>Add a Burger</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="ca">Burger Name:</label>
            <input type="text" id="burgerName" name="name">
        </div>


        <button type="submit">Add Burger</button>
    </form>

</div>